<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/windowBackground">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:background="@color/windowBackground">
                <LinearLayout
                    android:layout_weight="1"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:orientation="vertical"
                    android:background="@color/windowBackground">
                    <!--<TextView
                        android:layout_gravity="center"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Class"
                        android:textColor="@color/Gray"
                        android:textSize="22sp"
                        android:padding="10dp"/>
                    <View
                        android:layout_width="match_parent"
                        android:layout_height="1.8dp"
                        android:background="@color/fitproTheme"
                        />-->
                    <ListView
                        android:drawSelectorOnTop="true"
                        android:background="@color/windowBackground"
                        android:layout_marginBottom="5dp"
                        android:divider="@color/div"
                        android:dividerHeight="0dp"
                        android:id="@+id/classList"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        >
                    </ListView>
                </LinearLayout>

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="bottom|center"
                android:background="#fafafa"
                >
                <Button
                    style="@style/successBtn"
                    android:text="Create new"
                    android:id="@+id/createClasslistButton"
                    />
                <!--<Button
                    style="@style/cancelBtn"
                    android:text="delete"
                    android:layout_below="@+id/createClasslistButton"
                    android:id="@+id/exitClassListCButton"
                    />-->
            </RelativeLayout>

        </LinearLayout>
    <ProgressBar
        android:id="@+id/classListProgressBar"
        android:layout_gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
</FrameLayout>